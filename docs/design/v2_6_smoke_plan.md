# V2-6 スモーク（T1〜T5 × A・B、N=1）の走行計画

- 状態: 計画（走行はまだ）
- 前提: V2-5 は 2026-09-25 に complete（v2-dry-06、game-harness#83）。実装役は gemini-3.8-flash-medium で固定（裁定 2）。A の P2P の破壊は、プロンプトの差も含めた実験結果としてそのまま記録する（裁定 3）
- 目的: T1 だけでは確かめられないこと（T2 以降の B の秒の伸び、A の文脈の肥大と手番の破綻、前のタスクの振る舞いの保持）の一次情報を取る。**本走（V2-RUN、N=4）に進めるかの判断材料**を作る。A と B の優劣は N=1 では言わない

## 1. タスク（`experiments/v2/tasks.json`）

| タスク | 内容 |
|:--|:--|
| T1 | 左右移動（壁・盤面外の判定） |
| T2 | 自然落下と接地判定（ロック猶予・固定・次のミノの出現） |
| T3 | ハードドロップ（即時固定） |
| T4 | 回転（壁キックなし） |
| T5 | 壁キック回転（SRS） |

諮問にある「T2（ライン消去）」は、いまの T2 ではない（ライン消去は T1〜T5 に無い）。

## 2. 走らせ方

1. 走る前の確認（どれかが外れたら走らせない）
   - `C:\Users\tamar\AppData\Local\Temp\harness-narrow` の下が空
   - `python -m unittest discover -s tests` が OK
   - `projects/falling-blocks/pipeline.json` の実装役が `gemini-3.8-flash-medium`、TTL 300 秒
   - run-id `v2-smoke-01` の worktree が無い
2. 起動（A を T1〜T5 まで走らせてから B。乾式の走行と同じ順）

   ```
   python -m harness.ab.driver run --condition A --run-id v2-smoke-01 --manifest experiments/v2/tasks.json
   python -m harness.ab.driver run --condition B --run-id v2-smoke-01 --manifest experiments/v2/tasks.json
   ```

3. 集計：`python -m harness.ab.report --run-id v2-smoke-01 --cost-model experiments/v2/cost_model.json --dest experiments/v2/results/smoke/summary_smoke01.md`。呼び出しごとの手番・道具・外を指した手番・秒は、実装役のログとテレメトリから引数の要約だけを取り出す（中身は見ない）

既存の決まり（変えない）：

- 受入に落ちたタスクは失敗として記録し、次のタスクへ進む（2026-09-24 裁定）。**A が途中で落ちても走行は続く**ので、A の早期の脱落で T2〜T5 のデータが無くなることはない（落ちた状態の上に次のタスクが積まれる）
- 呼び出しの上限：A は 1 タスク 9 回（B の最大にそろえる）。B は 1 タスク 3 試行 × 内側ループ 3 ターン
- 前のタスクの終わりに落ちていたテストは、B の base の検査と P2P から外す（S2）。A の測定も同じ

## 3. 警戒指標と止める条件

### 3.1 実験を止める（ハーネスの欠陥。データが信用できなくなる）

| 条件 | 見る場所 | 扱い |
|:--|:--|:--|
| 作業場所の外を指した手番が 1 回でもある | 手番の `outside` | その条件の走行を止め、原因を直してから走り直す（v2-dry-05c の汚染の再発） |
| 走行の途中で `harness-narrow` の下にほかの作業場所が残っている | ディレクトリ | 同上 |
| 実装役の起動の失敗（rc 127、WinError） | 実装役のログ | 同上 |
| B の門の誤爆（実装の中身に関わらず落ちる。L6 の最低条件） | pipeline のログと verdict | 同上 |
| `PROPERTY_UNSATISFIABLE`（前提が成り立つ系列が無い。実装に依らない） | 反例の行 | 同上（宣言か生成器の欠陥。`v2_6_t5_acceptance.md`） |

### 3.2 止めずに記録する（実装役の振る舞い。これを測るのがスモークの目的）

| 指標 | 警戒の線 | 意味 |
|:--|:--|:--|
| B の実装役 1 回の秒 | 200 秒（TTL の 3 分の 2） | 諮問の懸念 1。T1 は 92.6 秒 |
| TTL の打ち切り（rc 124） | 1 回でも | L4 の再演。利用量は下限として残る |
| A の呼び出し 1 回の入力トークン | タスクごとの伸び（両対数の傾き） | 諮問の懸念 2。文脈の肥大 |
| 手番の数・`view_file`・`run_command` | 呼び出しごと | 空回りの戻り |
| A の受入の失敗 | タスクごと | 諮問の懸念 2。どのタスクで落ちたか |
| P2P の破壊 | タスクごと | 裁定 3。A の v2-dry-06 の 5 件（Issue17Cases）が T2 で直るか、持ち越すか |

### 3.3 費用の上限

- 見込み：v2-dry-06 の 1 タスクあたり A 0.1640・B 0.1940 USD から、タスクが重くなる分を見て **2〜5 USD**
- 最悪：両条件とも毎タスク上限の 9 回を呼び、1 回 0.44 USD（v2-dry-05c の A の 1 回）なら 5 × 9 × 0.44 × 2 ≈ 40 USD
- **上限 10 USD**。A を走らせた後に A の費用を集計し、5 USD を超えていたら B を走らせる前に止めて報告する（ドライバには走行の途中で費用で止める仕組みが無いので、条件の間で見る）

## 4. 本走（V2-RUN、N=4）に進む条件

スモークの後、次をすべて満たせば本走に進むことを提案する。満たさなければ、どの指標がどのタスクで外れたかを添えて次の手を出す。

1. 3.1 の条件に 1 度も当たらない
2. B の TTL の打ち切りが 0（1 回でもあれば、そのタスクの手番の内訳を見て、本走の前に対策する）
3. B が T1〜T5 を受入まで通す（N=1 なので 1 タスクの失敗は許すが、失敗の原因が門かハーネスなら 1 に当たる）
4. A の各タスクの結果（受入・P2P・打ち切り）が記録として揃う（A の成否は条件にしない。A が落ちるかどうかは実験が測るもの）

## 5. 報告に書くこと

- タスクごとの A・B の受入・試行・呼び出し・手番・秒・費用・P2P の破壊（`summary_smoke01.md`）
- 3.2 の各指標のタスクごとの推移
- A の呼び出しの入力トークンの伸び（傾き）
- 3.1 に当たったかどうか
- 本走に進むか（4 の判定）

## 6. v2-smoke-01 の結果（2026-09-25）

記録は `experiments/v2/results/smoke/notes.md`。

- **条件 A の T5 の途中で止めた**（§3.1：作業場所の外を指した手番が T5 a3 で 19、a4 で 18）。原因は再試行の指示に入った作業ツリーの絶対パス（dotnet の出力）。A はそれをたどって外の性質テストを読み、dotnet を走らせた。置き換えで塞いだ（B の前回の失敗にも同じ経路があったので、同じく塞いだ）
- **A の費用は止めた時点で 10.13 USD**（§3.3 の上限 10 USD と、B の前の線 5 USD を A だけで超えた）。B は走らせていない
- A は T1〜T4 を受入まで通した（T1 は P2P を 5 件壊し、T2 の後は 0）。T5 は 4 回呼んで受入に届かなかった
- 費用が伸びたのは、A が会話を積むので呼び出し 1 回の入力が伸びるから（T1 の 9.6 万 → T4 の 71 万 → T5 a4 の 225 万）。§3.3 の見込み（2〜5 USD）は、v2-dry-06（T1 だけ）の 1 タスクの費用を 5 倍しただけで、会話の伸びを入れていなかった。見込みの誤り

### 次に決めること

1. **費用の上限**：直した後に走り直すなら、A だけで T1〜T4 に 2.82 USD、T5 の最初の 1 回で約 1 USD かかる（v2-smoke-01 の実測）。T5 で再試行が重なれば 1 回 1〜3 USD。A・B 合わせて 10 USD に収まる見込みは低い。推奨：上限を 20 USD に上げ、A の後の中間の線を 12 USD にする。A の会話の伸びは、この実験が測るもの（単一チャット型の文脈の肥大）なので、A の形は変えない
2. **走り直しの run-id**：`v2-smoke-02`。v2-smoke-01 の A は記録として残す

## 7. 訂正：条件 A の費用（2026-09-25）

§6 の「A の費用は止めた時点で 10.13 USD」は誤り。agy の `result` の利用量は会話全体の累計で、会話を続ける A では呼び出しごとの `result` を足すと重複する。手番の利用量を足し直すと **3.16 USD**。§6 の「費用の上限を超えた」は取り消す（作業場所の外の手番で止めたことは変わらない）。記録は `experiments/v2/results/smoke/notes.md` の「訂正」。

## 8. v2-smoke-02 の結果（2026-09-25）

記録は `experiments/v2/results/smoke/notes.md`。

- A は T1〜T4 を受入 8/8・P2P の破壊 0・不変条件の違反 0 で通した（費用 2.07 USD）。T2 は最初の提出が 2/8 で、門の知らせで 6 回目に通った
- **T2 a5 で作業場所の外を指した手番が 4 つあり、§3.1 に当たった**。自分の会話の記録を読む（2）、作業場所の親と `%TEMP%` の一覧（2）。テストや他の走行のファイルの中身は読んでいない
- 総監督が止めたのは T5 の準備中で、T3・T4 は止めるべき後に走った（監視の知らせを待ちのコマンドで受けられなかった）
- B は走らせていない

### 次に決めること

1. **§3.1 の「作業場所の外」の線引き**。いまの線では、一覧だけの手番でも止まる。推奨：
   - 止める：作業場所の外の**ファイルの中身を読む**手番（view_file・Get-Content・Select-String・type など）と、**ビルドやテストを走らせる**手番（dotnet など）
   - 記録するだけ：作業場所の外の**一覧**（Get-ChildItem など）と、実装役の**自分の会話の記録**を読む手番（この PR で外に数えないようにした。ほかの会話の記録は外に数える）
2. **走り直し**：`v2-smoke-03` を A から。監視のスクリプトは欠陥を見つけたらドライバを自分で止める
3. 費用の上限は 20 USD のまま（訂正の後の A は T1〜T4 で 2.07 USD）

## 9. v2-smoke-03 の結果と本走への移行（2026-09-25）

記録は `experiments/v2/results/smoke/notes.md`、集計は `summary_smoke03.md`。

- A・B とも T1〜T5 を受入 8/8、P2P の破壊 0、不変条件の違反 0 で通した。止める条件には当たらなかった
- 費用（事前投資を除く）：A 3.07 USD、B 2.30 USD。秒：A 2,557.4、B 1,805.8
- §4 の 4 条件をすべて満たすので、**本走（V2-RUN、N=4）へ移行してよい**

### 本走に向けて

1. **T2 が TTL に近い**：B の 2 回が 289 秒台、A の 6 回が 200 秒を超えた。本走でも TTL は 300 秒のまま、打ち切りは実装役の振る舞いとして記録する（§3.2）。打ち切りが 1 回の走行で 2 回を超えたら、その時点で報告する
2. **費用**：1 走行（A＋B）で 5.37 USD。N=4 で約 21.5 USD。T2 の再試行の揺れを見て、**上限 40 USD、2 走行ごとに集計して 25 USD を超えていたら止めて報告**を提案する
3. **順序**：`run-all`（A と B を走行ごとに入れ替える）で時間帯の偏りを散らす
4. 監視は v2-smoke-03 と同じ（止める条件で自分でドライバを止める）
5. B の実装役の生ログをタスクごとに置く修正（この PR）を入れてから走らせる

## 10. v2-smoke-04 の結果と本走への移行（2026-09-25）

記録は `experiments/v2/results/smoke/notes.md`、集計は `summary_smoke04.md`。

- A・B とも T1〜T5 を受入 8/8、P2P の破壊 0、不変条件の違反 0。止める条件に 1 度も当たらず、外の一覧も 0
- T2 は両条件とも最初の提出で合格、1 回の呼び出し。T3 の差分は 41〜47 行になった。`PROPERTY_UNSATISFIABLE` は 0 回
- 費用：A 0.59 USD、B 1.07 USD（1 走行 1.65 USD）。最長の呼び出し 241.5 秒、TTL の打ち切り 0
- §4 の 4 条件をすべて満たす。**本走（V2-RUN、N=4）へ移行してよい**

### 本走の条件（§9 を更新）

- 費用：1 走行 1.65 USD（v2-smoke-04）〜5.37 USD（v2-smoke-03）の幅。**上限 30 USD、2 走行ごとに集計して 15 USD を超えていたら止めて報告**
- そのほか（TTL 300 秒のまま、`run-all` で A・B の順を入れ替える、監視は止める条件で自分で止める）は §9 のとおり
