# B（契約駆動の防壁型）の効率：実測の検証と改善の設計

- 状態: 調査の結果と設計。**2026-09-26 に §4・§5 の順序を改定し、改善を本走の前に入れた（§6、v2.2）**
- 前提: v2-smoke-03・04（`experiments/v2/results/smoke/`）。本走の前の見直し（game-harness#96）
- 諮問（2026-09-25）: B は A に対して恒常的に時間で 1.3 倍、費用で 2 倍劣後している。原因は B の実装上の無駄（① 毎呼び出しの巨大なプロンプトによる初手の長考、② 出力の 85〜95% が思考、③ 再試行でのプロンプトの過積載、④ プレフィックスキャッシュの敗北）。プロンプトの局所化・キャッシュの最適化・反例の圧縮・思考の抑止を調べよ
- 結論:
  1. **「恒常的に劣後」は実測と合わない**。v2-smoke-03 では B が費用で 25% 安く、秒で 29% 速かった。v2-smoke-04 では B が費用 1.81 倍・秒 1.32 倍。N=1 の 2 走行で向きが逆になる。ばらつきの主因は、両条件に共通の「最初の手番の思考の長さの非決定性」
  2. **① の病因（ステートレス＋巨大なプロンプトが長考を招く）は、実測で支持されない**。A も各タスクの最初の呼び出しで同じ大きさのプロンプト（3.2 万〜4.6 万字）を受け取り、T1 は A も B も履歴が無い。v2-smoke-03 では**同じ 3.2 万字のプロンプトで A の T1 が 37,547 トークン・114.5 秒考え**、B の T5 は 2,843 トークン。長考は条件によらず出る
  3. **② は事実**。費用の最大の項は思考の出力（v2-smoke-04 の B で 57%）。ただし A も同じ（出力に占める思考は A 84%・B 91%）。**減らす価値が最も大きいのはここ**で、手段はプロンプトの設計の実験（§5）
  4. **③ の病因（過去の試行ログや冗長なエラーが積み上がる）は事実と違う**。B の再試行の知らせは反例の 1 行（最大 5 行）で、T3 の 2 回目のプロンプトは 1 回目より 1,285 字増えただけ。263,658 トークンは 2 回の呼び出しの入力の合計で、その多くは agy が 1 回の呼び出しの中で手番ごとに会話を送り直す分（2 回目は 7 手番）と、2 回目の思考 65,228 トークン
  5. **④ は事実で、原因は B のプロンプトの並びではなく agy**。B の全 6 回の呼び出しで、最初の手番のキャッシュ読みは 0。agy に**同じプロンプトを新しい会話で 3 回**渡しても、キャッシュ読みは 3 回とも 0 で、入力のトークン数が毎回わずかに違った（13,285・13,293・13,295）。agy のシステムプロンプトに呼び出しごとに変わる部分があり、**プロンプトの並びを変えても会話をまたいだプレフィックスキャッシュは効かない**。効くのは 1 回の呼び出しの中（2 手番目以降）と、会話を続ける A の履歴だけ。取り逃している額は 1 走行で約 0.11 USD（B の費用の約 10%）

## 1. 事実（v2-smoke-04。`experiments/v2/tools/percall.py`。本文は見ず、字数・利用量・秒だけ）

### 1.1 呼び出しごと

| 条件 | タスク・回 | プロンプトの字数 | 最初の手番の思考 | 最初の手番の秒 | 思考の合計 | 入力 | キャッシュ読み | 手番 |
|:--|:--|:--|:--|:--|:--|:--|:--|:--|
| A | T1 1 | 31,669 | 2,773 | 12.8 | 7,414 | 40,596 | 53,161 | 3 |
| A | T2 1 | 35,296 | 12,355 | 46.2 | 12,710 | 39,561 | 151,511 | 3 |
| A | T3 1 | 39,863 | 2,771 | 11.3 | 2,870 | 114,201 | 155,564 | 3 |
| A | T3 2 | 1,069 | 21,485 | 75.1 | 35,219 | 28,732 | 389,325 | 4 |
| A | T4 1 | 42,388 | 2,604 | 21.4 | 2,712 | 27,037 | 368,997 | 3 |
| A | T5 1 | 45,542 | 1,782 | 9.3 | 1,864 | 20,985 | 281,439 | 2 |
| B | T1 1 | 31,640 | 24,264 | 76.5 | 25,235 | 81,286 | 49,097 | 3 |
| B | T2 1 | 35,704 | 39,373 | 110.7 | 41,475 | 102,553 | 478,732 | 8 |
| B | T3 1 | 40,342 | 9,400 | 33.8 | 9,727 | 47,747 | 98,150 | 4 |
| B | T3 2 | 41,627 | 13,122 | 46.4 | 65,228 | 215,911 | 275,881 | 7 |
| B | T4 1 | 42,955 | 3,674 | 13.9 | 3,708 | 34,370 | 28,630 | 2 |
| B | T5 1 | 45,282 | 2,434 | 12.3 | 2,462 | 34,064 | 28,628 | 2 |

- 各タスクの最初の呼び出しのプロンプトは、A と B でほぼ同じ大きさ（差は 600 字以内）。違いは、A の再試行が会話の続きで短い（1,069 字）のに対し、B の再試行は全部を渡し直す（41,627 字）こと
- 同じ字数（31,669・31,640）の T1 で、最初の手番の思考は A 2,773、B 24,264。v2-smoke-03 では逆で、A の T1 が 37,547・114.5 秒

### 1.2 費用の内訳（USD）

| 走行 | 条件 | 入力 | 出力（大半は思考） | キャッシュ読み | 合計 |
|:--|:--|:--|:--|:--|:--|
| v2-smoke-04 | A | 0.2033 | 0.2805 | 0.1051 | 0.5889 |
| v2-smoke-04 | B | 0.3869 | **0.6072** | 0.0719 | 1.0660 |

### 1.3 最初の手番のキャッシュ読み

| 条件 | 最初の手番の（入力, キャッシュ読み） |
|:--|:--|
| A | T1 (25,707, 0)、T2 (15,387, 36,859)、T3 (17,722, 69,602)、T4 (17,272, 111,696)、T5 (17,481, 132,252) |
| B | T1 (25,702, 0)、T2 (26,886, 0)、T3 (28,094, 0)、T3 の 2 回目 (28,458, 0)、T4 (28,841, 0)、T5 (29,522, 0) |

A は会話の履歴がキャッシュに乗り、新しいタスクの分（約 1.7 万トークン）だけが入力になる。B は毎回、agy のシステムプロンプトを含めて全部が入力になる。

### 1.4 agy のキャッシュの確認（2026-09-25、`gemini-3.8-flash-medium`、新しい会話を 3 回）

| 回 | 入力 | キャッシュ読み |
|:--|:--|:--|
| 1 | 13,285 | 0 |
| 2 | 13,293 | 0 |
| 3 | 13,295 | 0 |

同じプロンプトでも入力のトークン数が変わる（システムプロンプトに呼び出しごとに変わる部分がある）。会話をまたいだプレフィックスキャッシュは、プロンプトの側では作れない。

## 2. 要求ごとの実現可能性

| 要求 | 見立て | 期待できる効果 |
|:--|:--|:--|
| 1. プロンプトの局所化（Context Pruning） | できる。埋め込みの約 3 万字のうち、GddReference（約 1.3 万字）は T1〜T4 ではキック表を使わない。性質が参照する PR と、形の表だけに絞れる。**A にも同じ入力として当てる**（共有の入力。片方だけ変えると処置が混ざる） | 入力の費用は 1 呼び出し約 3,000 トークン減（1 走行で 0.02 USD 程度）。思考への効果は未知（§5 で測る） |
| 2. プレフィックスキャッシュ | **agy を使う限りできない**（§1.4）。会話を続ける（`--conversation`）と B が履歴を持つことになり、ステートレスの定義が崩れる。明示のキャッシュ（API の cachedContent）は agy を外す案で、2026-09-25 に却下済み | 取り逃しは 1 走行約 0.11 USD（B の約 10%）。上限として記録する |
| 3. 反例の圧縮 | **すでに最小**（反例の 1 行、最大 5 行）。増えた入力は agy の手番ごとの送り直しと思考 | 知らせの側で削れるものはほぼ無い |
| 4. 思考の抑止 | 実験で確かめる価値がある（費用の最大の項）。ただし、同じプロンプトで思考が 2,773〜37,547 と揺れるので、**1 回ずつの比較では効果を見分けられない**。同じプロンプトを何度も流す再生実験が要る（§5） | 未知 |

B に固有で、思考を誘いうる要素が 1 つある：B の指示にだけ、「受入テスト側に明らかな誤謬があり、自身の実装が正当と判断したら DISPUTE_TEST の JSON を出せ」という段落がある（A の固定テンプレートには無い）。テストの正しさを検討させる文で、長考の引き金になりうる。§5 の腕の 1 つにする。

## 3. B の設計として変えてよいもの・変えてはいけないもの

- **B だけの指示（DISPUTE の段落、指示の書き方）**は B の設計の一部なので、B を良くするために変えてよい。ただし A/B の比較では処置の一部として報告する
- **共有の入力**（単位定義の prompt・契約・参照データ・埋め込み・作業場所の決まり）を変えるときは、A と B に同じく当てる
- 門・性質・受入は、効率のためには変えない

## 4. 本走（V2-RUN）への影響

> **2026-09-26 に改定（§6）**。下の「今の形のまま本走」は撤回した。特定した改善を先に入れ、v2-smoke-05（N=1）で健全性を確かめてから、改善した形で本走（N=4）を 1 回で走らせる。4 走行が同じ処置であるという要件は、本走の前に入れれば満たされる。

本走は、校正した今の形（v2-smoke-04 と同じ）のまま走らせる。ここでの改善は、本走の結果を比べる対象（v2.2 の B）として、本走の後に入れる。本走の途中で B を変えると、4 走行が同じ処置でなくなる。

## 5. 本走の後の実験と実装のロードマップ

### E1：思考の再生実験（費用の見込み 5 USD 前後）

1. 道具：実装役の呼び出しを、保存したプロンプトと作業場所の写しで N 回流す（`harness/ab/replay.py`。実装役の出力は捨て、利用量・手番・秒・編集の有無だけを残す）。正しさは、有望な腕だけ後で判定（`pipeline.judge`）に通す
2. 対象：v2-smoke-04 の B の T1・T2 の最初の呼び出し（長考が出た 2 つ）
3. 腕（各 N=8）：
   - 0：いまのプロンプト
   - 1：DISPUTE の段落を外す
   - 2：GddReference をタスクが参照するものに絞る（要求 1）
   - 3：作業場所の決まりの結びを「埋め込んだ中身で足りる。最初に編集の道具を呼ぶ」にする（要求 4。禁止ではなく手順の事実）
4. 指標：最初の手番の思考、思考の合計、呼び出しの秒、手番、編集したか。中央値と四分位で比べる
5. 判定：中央値で 3 割以上減り、編集しなかった回が増えない腕を採る

### E2：採った腕を v2.2 の B として入れ、スモーク（N=1）→ 本走と同じ形の走行（N=4）で、本走の B と比べる

- 共有の入力を変えた腕（2）を採るなら、A も同じ入力で走らせ直す
- 比べるもの：費用・秒・思考・受入・P2P・門の効果。安全性（門）は変えないので、受入と P2P が落ちないことが条件

### 見込み

- 思考が費用の 57%（B）。E1 で中央値が 3 割減れば、B の費用は約 17% 減る
- キャッシュは agy の制約で取れない（約 10% が上限として残る）
- 局所化の入力の削減は 2% 程度で、効果の中心は思考への影響（E1 の腕 2）

## 6. 改定（2026-09-26）：改善を本走の前に入れる（v2.2）

### 6.1 方針

§4・§5 は「今の形で本走 → E1（再生実験、約 5 USD）→ E2（改善の本走、約 13 USD）」だった。改善の候補が特定済みで、
本走の途中ではなく前に入れれば 4 走行の処置は揃うので、順序を改めた：

1. 改善（下の 4 点）を入れる（この PR）
2. v2-smoke-05（T1〜T5 × A・B、N=1）で健全性（受入・P2P）と、最初の手番の思考と秒を見る
3. 改善した形で本走（V2-RUN、N=4）

E1 の腕 1〜3 を個別に測らず、まとめて入れる。代わりに、**どの腕が効いたかは切り分けられない**。

### 6.2 入れたもの

| # | 変更 | 場所 | 条件 |
|:--|:--|:--|:--|
| 1 | DISPUTE_TEST の段落を外す（E1 の腕 1） | `harness/pipeline.py` の `call_implementer` | B だけ（B の設計の一部。§3） |
| 2 | GddReference の抜粋を埋め込む（E1 の腕 2）。残すのは、単位定義の prompt が参照する PR、書き換えてよいファイルがすでに使う定数と表、形の表（Shape）と、残した表が引く定数。外した表は宣言だけ残す。作業場所に置くファイルは全部のまま | `harness/implementer_context.py` の `prune_reference`・`for_unit` | A・B 共通（共有の入力） |
| 3 | 作業場所の決まりの結びを手順の事実にする（「埋め込んだ中身で、作業には足ります」「最初の手番で、直ちに編集の道具（replace_file_content・write_to_file など）を呼び…」）。B も A と同じく、プロンプトの末尾に置く（E1 の腕 3） | `harness/tool_policy.py`、`harness/pipeline.py` | A・B 共通 |
| 4 | `break_even` は、A の当てはめの傾き α ≤ 0 なら外挿しない | `harness/ab/report.py` | 集計 |

DISPUTE_TEST の読み取り（`pipeline` の One-Strike）は残した。指示に無いので出ないはずだが、出たときの扱いは変えない。

### 6.3 抜粋の大きさ（実測。v2prep が作る GddReference、字数だけ）

| タスク | 全部 | 抜粋 | 残した定数 | 回転補正候補の表 |
|:--|:--|:--|:--|:--|
| T1 | 13,068 | 6,906 | 29 | 宣言だけ |
| T2 | 13,068 | 7,057 | 31 | 宣言だけ |
| T3 | 13,068 | 7,063 | 31 | 宣言だけ |
| T4 | 13,068 | 6,906 | 29 | 宣言だけ |
| T5 | 13,068 | 11,288 | 46 | 本体あり |

T1〜T4 で 1 呼び出しあたり約 6,000 字減る。§2 の見立てどおり、入力の費用への効果は小さい。効果の中心は思考への影響で、未知。

### 6.4 break_even の是正

v2-smoke-04 の `summary_smoke04.md` の「N ≤ 10000 では回収しない」は、A の走行コストの列（T3 の再試行が高く、T4・T5 が安い）
に a·k^α を当てはめた傾きが負（α = −0.19）で、A の 1 タスクの費用が減り続けると外挿した結果である。これは文脈の累積による
伸びの推定ではなく、タスクの難しさの並びの効果なので、α ≤ 0 のときは外挿せず、その旨を書く。
A の伸びを O(N²) と仮定して外挿することはしない（実測の 5 タスクでは、それを支える値が無い）。

### 6.5 v2-smoke-05 の見方（N=1 の限界）

- **健全性**：受入 8/8 の維持、P2P の破壊 0、テストの改ざん 0。これは N=1 で判定できる
- **思考の削減**：§1.1 のとおり、同じプロンプトで最初の手番の思考が 2,773〜37,547 トークンと揺れる。N=1 の比較で減っても
  増えても、改善の効果とは言い切れない。v2-smoke-05 では「最初の手番で編集の道具を呼んだか」「思考だけで終わった呼び出し
  （error_message）の数」「最初の手番の思考と秒」を記録し、効果の判定は本走（N=4）の中央値で行う
