# v2 の乾式の走行（T1、A・B を 1 回ずつ）

- 入力：`experiments/v2/tasks.json`（契約と性質は V2-4 の宣言、単位定義は `harness/ab/v2prep.py` が機械的に作った）
- 起動：`python -m harness.ab.driver run --condition <A|B> --run-id <id> --through T1 --manifest experiments/v2/tasks.json`
- 集計：`summary.md`（v2-dry-02）と `summary_dry01.md`（v2-dry-01）。`python -m harness.ab.report --cost-model experiments/v2/cost_model.json`

## v2-dry-02（whitelist を直した後）

| 条件 | 受入（公開＋非公開） | 受入（公開） | 試行 | 実装役の呼び出し | P2P の破壊 | 不変条件の違反 | 追加 / 削除 | 秒（条件の仕事） | USD |
|:--|:--|:--|:--|:--|:--|:--|:--|:--|:--|
| A | 8/8 | 4/4 | 1 | 1 | 0 | 0 | 114 / 4 | 149.6 | 0.3156 |
| B | 8/8 | 4/4 | 2 | 4 | 0 | 0 | 98 / 3 | 1267.1 | 不明（分かっている 2 回で 1.0547） |

B の pipeline の中（`T1.pipeline.json`）：

| 段 | 結果 |
|:--|:--|
| 基準の確立 | 105.8 秒。実装前は生成物がビルドできないので、生成物 4 ファイル（探針・参照モデル・判定・T1 の性質テスト）を除いて測り直した。P_base 31 件 |
| 試行 1 | 実装役 1 回（245.2 秒）→ 内側ループの高速検査に合格 → **whitelist の門で RETRY**（実装役が whitelist の外の `Board.cs` を編集した） |
| 試行 2 | 実装役 3 回。1・2 回目は TTL（300 秒）で打ち切り（rc=124、利用量なし）、3 回目（291.0 秒）で高速検査に合格 → 静的・差分の予算・受入（9.5 秒）→ **非公開シード（20 本、性質 4 件）に合格**（1.2 秒）→ 不変条件（5.3 秒）→ 持ち出し |

観測の要点：

1. **配管は通った**：A・B とも、プロンプトが渡り、実装役が C# を編集し、測定器が公開と非公開の両方のシードで受入を測った（非公開シードは走行とタスクから決め、実装役には渡していない）
2. **B の門は動いた**：基準の確立の生成物の除外、whitelist（`Board.cs` の編集を弾いた）、内側ループの高速検査、受入、非公開シード、不変条件がそれぞれ働いた。契約の探針が落ちる回（ビルドの失敗）と、公開シードの反例の 1 行が返る回は、今回は起きなかった（実装役が 1 回目から通したため）。どちらも単体テストでは確かめている
3. **B は遅い**：条件の仕事の秒は A の約 8.5 倍（1267.1 / 149.6）。内訳は、基準の確立 105.8 秒、whitelist で捨てた試行 1 の 248.5 秒、試行 2 の実装役の 891.3 秒（うち TTL の打ち切り 2 回で 600.2 秒）
4. **実装役の 1 回の呼び出しが 245〜300 秒**：編集だけの指示（v2 §7）にしても、Flash の 1 回は 30〜60 秒（§10 の 2 の目標）に収まらなかった。A の 1 回は 149.6 秒（測定を除いた条件の仕事の秒）。B は TTL に 2 回当たり、その 2 回の利用量が残らないので、B のトークンと費用は不明（分かっている 2 回だけで 1.0547 USD。A は 0.3156 USD）

## v2-dry-01（whitelist を直す前）

- A：受入 8/8、1 回の呼び出し、220.6 秒
- B：試行 1・2 とも静的な門の「Game/Assets/Core/MinoShape.cs が存在しません」で RETRY（v1 の名残の whitelist。`gate_static_common` は whitelist のファイルがすべて在ることを要求する）。試行 3 の途中で人間が止めた
- 直し方：単位定義の whitelist と impl_files を `Game/Assets/Core/GameState.cs` だけにした（`harness/ab/v2prep.py`）

## v2-dry-03（v2.1：stream-json・ファイルの埋め込み・whitelist に Board.cs・内側の whitelist の検査・--sandbox）

集計は `summary_dry03.md`。

| 条件 | 受入（公開＋非公開） | 試行 | 実装役の呼び出し | 手番 | 入力 | 思考 | 出力 | キャッシュ読み | 実装役の秒 | 条件の仕事の秒 | USD |
|:--|:--|:--|:--|:--|:--|:--|:--|:--|:--|:--|:--|
| A | 8/8 | 1 | 1 | 26 | 220,272 | 26,101 | 32,004 | 1,135,060 | — | 251.6 | 0.3703 |
| B | 8/8 | 1 | 1 | 27 | 367,408 | 33,398 | 39,339 | 1,045,139 | 213.3 | 348.1 | 0.5015 |

- B は 1 回の試行・1 回の呼び出しで通った。TTL の打ち切りは 0 回（v2-dry-02 は 2 回）。条件の仕事の秒は 1267.1 → 348.1。内訳は基準の確立 114.8 秒、試行 231.6 秒（うち実装役 213.3 秒）
- 両条件とも、実装役は whitelist の中（GameState.cs と Board.cs）だけを書き換えた。内側の whitelist の検査は一度も働かなかった
- `--sandbox` のもとでも、編集の道具（`write_to_file`・`replace_file_content`）は使えた（裁定 4 の確認）
- 利用量は、両条件とも `result` まで届いたので下限ではない

### 手番の内訳（道具の名前と引数のファイル名・コマンドだけを数えた。中身は見ていない）

| 道具 | A | B |
|:--|:--|:--|
| view_file | 9 | 13 |
| run_command | 7 | 4 |
| manage_task | 7 | 4 |
| schedule | 0 | 3 |
| write_to_file・replace_file_content | 2 | 2 |

- 読んだファイル：base の既存の型（ActiveMino・MinoType・Rotation・GamePhase。B）、生成したテスト（性質テスト・参照モデル・判定・探針）、既存のテスト（Issue12Cases・Issue17Cases）、構造化仕様（`docs/spec/spec.md`）と GDD（`docs/gdd/source.md`）、B は単位定義（`tools/units/issue_12.json`・`issue_17.json`）と、埋め込んだはずの GameState.cs も読み直した
- 端末：ディレクトリの一覧（再帰を含む）と `git diff --stat`。どれも読むだけ。`--sandbox` はこれらを止めない
- 目標（改善計画 §4）との比較：手番 26〜27（目標 3 以下）、入力 22 万〜37 万（8 万以下）、実装役の秒 213.3（B。90 以下）。**どれも未達**

## v2-dry-04（v2.1b：base の既存の型と仕様の抜き出しの埋め込み・指示の厳格化）

集計は `summary_dry04.md`。

| 条件 | 受入（公開＋非公開） | 受入（公開） | 試行 | 呼び出し | 条件の仕事の秒 | USD |
|:--|:--|:--|:--|:--|:--|:--|
| A | **7/8**（非公開で 1 件落ちた） | 4/4 | 2 | 2 | 259.0 | 0.6612 |
| B | 8/8 | 4/4 | 1 | 3（内側ループ） | 389.0 | 0.4859 |

呼び出しごと（手番・道具は stream-json から。B の 1・2 回目はテレメトリ、それ以外は実装役のログ）：

| 条件・回 | 手番 | 道具 | 入力 | 思考 | 出力 | 秒 | プロンプトの字数 |
|:--|:--|:--|:--|:--|:--|:--|:--|
| A 1 | 3 | write_to_file 2 | 77,061 | 47,091 | 50,956 | — | — |
| A 2 | 2 | write_to_file 1 | 113,366 | 73,945 | 80,978 | — | — |
| B 1 | 3 | replace_file_content 2 | 112,609 | 31,683 | 36,873 | 108.2 | 22,714 |
| B 2 | 3 | view_file 1・replace_file_content 1 | 87,559 | 33,021 | 33,720 | 112.5 | 28,961 |
| B 3 | 2 | replace_file_content 1 | 36,865 | 9,772 | 10,185 | 37.8 | 29,232 |

- **探索は止まった**：どの呼び出しも手番 2〜3。`manage_task`・`schedule`・`run_command` は 0 回、`view_file` は B の 2 回目の 1 回だけ（v2-dry-03 は A 26・B 27 手番）
- B は内側ループの 3 ターン目で合格（1・2 ターン目は高速検査で落ち、反例を受けて直した）。whitelist の内側の検査は働かなかった（外への変更なし）。非公開シード（20 本）にも合格
- A は、試行 2 で公開シードの 4 件を通して止まったが、最後の測定の非公開シードで 1 件落ちた。**非公開シードが公開シードへの合わせ込みを捕まえた**
- 目標（改善計画 §4）：手番 3 以下は全呼び出しで達成。入力 8 万以下は A 1・B 3 だけ（A 2 は会話を積むので 11 万）。90 秒以下は B 3 だけ（B 1・2 は 108〜113 秒）
- **いまは思考が重い**：A 2 は出力 80,978 のうち思考 73,945。手番が減ったので、秒と費用を支配しているのは思考の量になった。裁定 3（`--effort` の比較）を行える状態になった
- B の実装役のログは、1 試行の中の呼び出しごとに上書きされ、最後の 1 回分しか残っていなかった（手番の記録はテレメトリに全部ある）。呼び出しごとにファイルを分けるよう直した（`implementer_attempt_<n>_call<k>.log`）

## v2-dry-05（v2.1c：参照データの契約化・細い作業場所・禁止の頼みの全廃・gemini-3.8-flash-medium）

集計は `summary_dry05.md`（v2-dry-05c）。走行は 3 回に分かれた。

| 走行 | 何が起きたか | 扱い |
|:--|:--|:--|
| v2-dry-05 | `--model gemini-3.8-flash-high` と `--effort medium` を agy が併用させず、全呼び出しが 0 秒で失敗 | 無効。出力は `C:\src\.local\out\ab\v2-dry-05-invalid` に退避。思考の重さはモデル名（`gemini-3.8-flash-medium`）で選ぶよう直した |
| v2-dry-05b | B の内側ループの 2 回目以降（反例つき、プロンプト 3.3 万字）が Windows のコマンドラインの上限を超えて起動できなかった（WinError 206）。4 回の呼び出しが 0 秒・利用量不明 | B は無効。プロンプトを標準入力で渡すよう直した。A の記録だけ参考に残す |
| v2-dry-05c | 両条件とも最後まで走った | 本記録 |

| 条件 | 受入（公開＋非公開） | 試行 | 呼び出し | 手番 | 入力 | 思考 | 出力 | キャッシュ読み | 実装役の秒 | 条件の仕事の秒 | USD |
|:--|:--|:--|:--|:--|:--|:--|:--|:--|:--|:--|:--|
| A | 8/8 | 1 | 1 | 30 | 309,504 | 24,292 | 30,340 | 1,191,535 | — | 255.9 | 0.4353 |
| B | 8/8 | 1 | 1 | 33 | 329,004 | 15,254 | 22,060 | 1,309,562 | 270.3 | 403.5 | 0.4277 |

手番の道具（名前だけを数えた）：

| 道具 | A | B |
|:--|:--|:--|
| run_command | 10 | 14 |
| manage_task | 10 | 14 |
| view_file | 7 | 2 |
| replace_file_content | 2 | 2 |

- **裁定 3 の基準は満たした**：受入は両条件とも 8/8。思考は v2-dry-04 の A 121,036（2 回の合計）→ 24,292、B 74,476（3 回の合計）→ 15,254。呼び出し 1 回あたりでも A 47,091 → 24,292（48% 減）、B 24,825 → 15,254（39% 減）
- **探索の手番が戻った**：禁止の頼みを外したら、手番は 2〜3（v2-dry-04）から 30〜33 になった（v2-dry-03 の 26〜27 と同じ水準）。増えたのは端末の操作と作業の管理（`manage_task`）。細い作業場所には書き換えてよいファイルと契約と既存の型しか無いので、読めたものはそれだけ（テスト・仕様書は無い）。何のコマンドだったかは、手番の記録に道具の名前しか残していないので分からない
- 入力は A 19 万 → 31 万、B 24 万 → 33 万に増えたが、思考（出力の単価）が減ったので、費用は A 0.6612 → 0.4353、B 0.4859 → 0.4277 USD に下がった。実装役の 1 回の秒は 108〜113 → 270.3 に伸びた
- B は 1 回の試行・1 回の呼び出しで、非公開シード（20 本）にも合格。書き戻したのは `GameState.cs`・`Board.cs` だけで、whitelist の内側の検査は働かなかった
- v2-dry-05b の A（参考）：手番 13、受入 8/8 だが **P2P を 5 件壊した**（Issue17Cases の自然落下・出現位置・開始のティック）。前のタスク（falling-blocks#17）の振る舞いは性質の宣言に無く、base のコードの中にしか無い。A には門が無いので、そのまま測定まで進んだ。v2-dry-05c の A は 0 件
