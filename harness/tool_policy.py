"""実装役に許す道具（docs/design/v2_contract_foundry.md §7、v2.1b：docs/design/v2_1_improvement_plan.md §5）。

A/B 実験の両条件（driver の条件 A と pipeline）が同じ文面を使う。

**再試行を含めて、実装役は編集だけ。** ビルド・テスト・探索は一律で使わせない。検証は外側の門が行い、
失敗は反例（PROPERTY_FAIL の 1 行）として次の呼び出しで渡す。v1 は再試行で道具を解禁していたが、
b4-smoke-02 の T4 で 9 回の呼び出しがすべて TTL（300 秒）で打ち切られた（game-harness#63）ので廃止した。

**v2.1b**：v2-dry-03 では、編集だけと頼んでも、実装役はテスト・仕様書・GDD・単位定義を読み、ディレクトリを一覧し、
作業の管理（manage_task・schedule）を使って 26〜27 手番を回した。v2.1b は読むもの・使う道具を名指しで禁じた。

**v2.1c**（docs/design/v2_1c_structure_review.md §1.3、2026-09-25 の裁定 2）：禁止の頼みは全廃した。実装役は
細い作業場所（書き換えてよいファイルと契約と既存の型だけ。harness/narrow_dir.py）で動くので、テストも仕様書も
そこに無い。文面は、検証がどこで行われるかと、前提がどこにあるかという事実だけを書く。

**v2.1d**（docs/design/v2_1d_env_protocol.md、2026-09-25 の方針）：v2-dry-05c では手番が 30〜33 に戻った。
引数を調べると、空回りは「作業場所の一覧・検索（ほかの走行の作業場所まで）」「仕様 ID（ST-09 など）の定義探し」
「テスト・ログ・csproj・dotnet・git の確認」の 3 種で、manage_task はすべて直前の端末の操作の完了待ち（status）だった。
禁止ではなく、作業場所の決まり（何がどこにあり、何が無く、検証がどこで行われるか）を事実として示す。

**v2.2**（docs/design/v2_b_efficiency.md §6）：結びを手順の事実にした（埋め込んだ中身で足りる。最初に編集の道具を呼ぶ）。
v2-smoke-04 では、最初の手番で編集せずに思考だけで終わる呼び出しがあった。両条件とも、プロンプトの末尾に置く。
GddReference は、その仕事に関わる部分の抜粋を埋め込む（implementer_context.prune_reference）。

**v2.3**（docs/design/v2_2_architecture_self_critique.md §4.1 の案 1、F5）：4 か所に散っていた同じ趣旨の指示
（pipeline の作業場所の段落と「計画を返さず」、A のテンプレートの受入と答え方、COMMON の受入の説明）をここに一本化した。
"""

PROTOCOL = (
    "## 作業場所の決まり（事実）\n\n"
    "- 作業場所にあるファイルは、「いまのファイルの中身」の節に並べたものがすべてで、この仕事に要る中身はそこに埋め込んであります"
    "（GddReference は、この仕事に関わる定数と表の抜粋）。"
    "作業場所の外（一時ディレクトリのほかのフォルダを含む）に、この仕事に関わるものはありません\n"
    "- ビルドとテストの道具（dotnet・テストプロジェクト・ログ・git）は作業場所にありません。"
    "ビルドと性質テスト（前提が成り立つどの Tick でも帰結が成り立つかを、ランダムな開始状態と入力の列で確かめる）による"
    "検証は外側のパイプラインが行い、失敗は次の指示で反例として伝えます\n"
    "- RL-xx・ST-xx・LP-xx などの ID は、性質の出典を示すラベルです。この仕事での意味は、書いてある性質（前提と帰結）がすべてです。"
    "PR-xx は GddReference の定数です\n"
    "- ですから、ファイルの一覧・検索・読み直しのための端末のコマンドも、manage_task・schedule も要りません。"
    "埋め込んだ中身で、作業には足ります\n"
    "- 手順：計画・実装案・確認は返しません。最初の手番で、直ちに編集の道具（replace_file_content・write_to_file など）を呼び、"
    "書き換えてよいファイルを編集してください。編集したら、変更したファイルの一覧だけを答えて、そのまま終えてください")
EDIT_ONLY = PROTOCOL


def text():
    """実装役への道具の指示。最初の呼び出しでも再試行でも同じ。"""
    return EDIT_ONLY
